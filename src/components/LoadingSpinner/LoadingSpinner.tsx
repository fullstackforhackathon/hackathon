import * as React from 'react';
import { ScaleLoader } from 'react-spinners';
import "./loadingSpinner.scss";

export default (props: any) => <div className="scaleLoaderContainer"><ScaleLoader color="rgba(103, 58, 183, 1)" /></div>
